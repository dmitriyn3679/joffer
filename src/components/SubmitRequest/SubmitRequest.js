import classes from "./SubmitRequest.module.css";
import {Button} from "@mantine/core";

export const SubmitRequest = () => {
  return (
    <Button className={classes.submit}>Залишити заявку</Button>
  );
};
